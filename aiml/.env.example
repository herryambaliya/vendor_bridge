# Port to run the FastAPI service on
PORT=8000
HOST=127.0.0.1

# Environment setting (development / production)
ENV=development

# Allowed CORS origins (separated by commas)
ALLOWED_ORIGIN=http://localhost:3000
